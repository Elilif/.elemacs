# -*- mode: snippet -*-
# name: \expression
# key: ep
# condition: (and (eli/tex-algorithm-p) (quote auto))
# group: algorithm
# --
`(if (eq major-mode 'org-mode) "\\(" "$")`$0`(if (eq major-mode 'org-mode) "\\)" "$")`\\;