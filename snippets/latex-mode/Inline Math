# -*- mode: snippet -*-
# key: mk
# name: Inline Math
# condition: (and (not (eli/texmathp)) (quote auto))
# group: math
# --
`(if (eq major-mode 'org-mode) "\\(" "$")`$0`(if (eq major-mode 'org-mode) "\\)" "$")`